name: lan_sync_core
description: Offline-first multi-device synchronization on local area networks (LAN). Peer-to-peer data sync without a central server.
version: 0.1.0-dev.2
repository: https://github.com/HardikSJain/lan_sync_core
homepage: https://github.com/HardikSJain/lan_sync_core
issue_tracker: https://github.com/HardikSJain/lan_sync_core/issues

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  crypto: ^3.0.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
